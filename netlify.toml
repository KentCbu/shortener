[build]
  command = "npm install"  # Optional: Kung may dependencies
  publish = "."           # Gamitin ang root folder

[functions]
  directory = "api"       # Netlify Functions folder
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
